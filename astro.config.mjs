// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://this-is-r-gaurav.github.io', // Replace with your real username
    base: '/',                         // Host at root level
    server: {
        port: 3500
    }
});
